#include <stdio.h>
int main()
{
    char ch;
    printf ("Enter an alphabet: ");
    scanf ("%c",&ch);
    if (ch>='A' && ch<='Z')
        printf ("Alphabet '%c' is in Uppercase\n",ch);
    else if (ch>='a' && ch<='z')
        printf ("Alphabet '%c' is in Lowercase\n",ch);
    else
        printf ("%c is not an alphabet\n",ch);
    return 0;
}
